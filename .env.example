BOT_TOKEN=1234567890:AA...
# Telegram user IDs, comma-separated (admins can open /admin)
BOT_ADMINS=123456789,987654321

# Polling interval (seconds) for Hyperliquid monitoring
HL_POLL_INTERVAL_SECONDS=10

# Logging level: DEBUG or INFO
LOG_LEVEL=INFO

# Data paths inside container (or local run). Keep default for Docker volumes.
DATA_DIR=./data
DB_PATH=./data/db/app.sqlite3
LOG_DIR=./data/logs
